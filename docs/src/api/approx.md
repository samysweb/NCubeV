# Approx

Linearization and over-approximation utilities (OVERT-backed) for generating piecewise
linear constraints and bounds used by Mosaic.

```@meta
CurrentModule = Main.NCubeV.Approx
```

## API

```@autodocs
Modules = [Main.NCubeV.Approx]
Order   = [:module, :type, :function]
Private = true
```
