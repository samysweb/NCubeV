# Util

Small helpers for logging, rounding, and common data structures used throughout the codebase.

```@meta
CurrentModule = Main.NCubeV.Util
```

## API

```@autodocs
Modules = [Main.NCubeV.Util]
Order   = [:module, :type, :function]
Private = true
```
