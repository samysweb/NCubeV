# Config

Central configuration knobs: solver selection, timers, and other global settings.

```@meta
CurrentModule = Main.NCubeV.Config
```

## API

```@autodocs
Modules = [Main.NCubeV.Config]
Order   = [:module, :type, :function]
Private = true
```
