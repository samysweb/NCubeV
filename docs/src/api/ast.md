# AST

Abstract syntax tree for terms, formulas, and constraints, including operations and
combinators used across the pipeline.

```@meta
CurrentModule = Main.NCubeV.AST
```

## API

```@autodocs
Modules = [Main.NCubeV.AST]
Order   = [:module, :type, :function]
Private = true
```
