# Parsing

File-based and IO-based parsing for specifications and constraints using Tokenize.jl.
Provides term/formula parsing and a small helper wrapper around the token stream.

```@meta
CurrentModule = Main.NCubeV.Parsing
```

## API

```@autodocs
Modules = [Main.NCubeV.Parsing]
Order   = [:module, :type, :function]
Private = true
```
