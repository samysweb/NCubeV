# Control

Orchestration of the end-to-end pipeline: prepare queries, run generation/approximation,
call verifiers and SMT filters, and aggregate results.

```@meta
CurrentModule = Main.NCubeV.Control
```

## API

```@autodocs
Modules = [Main.NCubeV.Control]
Order   = [:module, :type, :function]
Private = true
```
