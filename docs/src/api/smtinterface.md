# SMTInterface

Abstractions and helpers over SMT backends (Z3, CVC5, …), AST translation utilities, and
star-based counterexample filtering per Mosaic.

```@meta
CurrentModule = Main.NCubeV.SMTInterface
```

## API

```@autodocs
Modules = [Main.NCubeV.SMTInterface]
Order   = [:module, :type, :function]
Private = true
```
