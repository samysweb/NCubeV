# NCubeV (top-level)

Brief description

- Orchestrates all submodules: parsing, analysis, approximation, query generation, solver interfacing, and verifiers.
- Exposes the CLI entrypoint and minimal top-level helpers.

```@meta
CurrentModule = Main.NCubeV
```

## API

```@docs
NCubeV.main_NCubeV
NCubeV.run_cmd
```

