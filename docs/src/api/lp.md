# LP

Lightweight helpers around JuMP/GLPK for feasibility checks and per-dimension optimizations.

```@meta
CurrentModule = Main.NCubeV.LP
```

## API

```@autodocs
Modules = [Main.NCubeV.LP]
Order   = [:module, :type, :function]
Private = true
```
