# Verifiers

Verifier integration and registry; includes NNEnum-based enumeration modes and a no-op fallback.

```@meta
CurrentModule = Main.NCubeV.Verifiers
```

## API

```@autodocs
Modules = [Main.NCubeV.Verifiers, Main.NCubeV.Verifiers.Registry, Main.NCubeV.Verifiers.NNEnum]
Order   = [:module, :type, :function]
Private = true
```
