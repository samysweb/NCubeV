# VerifierInterface

Shared types bridging the pipeline and verifiers (queries, results, stars, statuses).

```@meta
CurrentModule = Main.NCubeV.VerifierInterface
```

## API

```@autodocs
Modules = [Main.NCubeV.VerifierInterface]
Order   = [:module, :type, :function]
Private = true
```
